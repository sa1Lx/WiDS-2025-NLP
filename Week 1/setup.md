I am using colab extension in VSCode for running Jupyter Notebooks. While that is the simplest method, you can install the required packages in a venv or in your global environment:  

Add this to your directory: [`requirements.txt`](requirements.txt)

```bash
pip install -r requirements.txt
```